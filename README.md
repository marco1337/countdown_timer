# Project Title
Countdown timer using C++ and the raylib library

## How to Use
### Compile the project
Right now the project only compiles on MacOS using clang++. You can run compile_timer.sh to compile the main.cpp file with the compiled raylib library for a Mac architecture.